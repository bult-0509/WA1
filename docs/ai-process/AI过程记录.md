# AI过程记录

## 2026-09-04 开发启动

用户原文：

> 开始按计划书执行

这条指令明确授权按当前v1.1精简规格实施，具有确认需求并开始开发的含义，不要求用户重复固定口令。实际工具为OpenAI Codex。四人成员分工各25%是计划责任；目前程序由Codex生成，不能写成四人已分别手工编写。

实施顺序：环境与数据准备 → 数据处理/统计 → 六页界面/离线地图 → 聚类/测试 → 便携包 → 实际截图与课程材料。另一台实体电脑实测、学号补填和邮件发送需按实际条件记录，不能模拟完成。

此前规格v1.0及用户精简反馈已留档。当前对话原始记录按阶段保存于raw/，它可能含工具输出，不直接作为正式报告全文；报告引用原始Prompt和关键修改并注明范围。第一版代码在首次测试前保存快照，后续问题与修复追加记录。

## 实施与验证

- 实际环境：CPython 3.13.15 Windows x64，49项依赖精确锁定。原始3788892行处理为小时、站日和全网表；正常117天，合法零值保留，六个缺损日标记。
- 首轮页面测试出现地图站名列冲突；保留失败日志后，只按ID合并指标，13项测试通过。
- 浏览器发现短日期轴识别问题，改用分类日期轴；界面保留清楚的业务筛选，移除开发参数；聚类代表站改为按距组中心排序。
- 浏览器实际切换六页和三个模式、检查桌面及窄屏布局；PNG按钮反馈生成成功。下载文件路径未由内置浏览器返回，后续异机需确认文件落盘与打开，不能把按钮成功当成完整文件验收。
- 便携包移至中文与空格目录，只加载随包依赖，验证六页与三种模式。启动/关闭服务真实执行，Windows提示与打开浏览器由测试替身确认；这不是另一台电脑的人工断网验收。
- DOCX使用老师原封面/正文样式生成；独立调研正文均超过1500汉字，各5条真实参考。PPT12页实际渲染，修复小数自动SI缩写，逐页检查。
- Word逐页渲染因缺少soffice不可用，结构检查完成，视觉验收明确待补。
- 最终成果、首版代码、需求历史和日志一并保留；交付对话为可见消息，不附内部推理、隐藏系统指令或机器元数据。
