# 测试与验收记录

日期：2026-09-04。执行者：OpenAI Codex。

## 已验证

- Python 3.13.15随包环境及八项核心库成功安装、导入，数据处理完整运行。
- 原始3,788,892行处理为631,482行小时表、37,146行站日表，正常分析117日；结果总计约8.4MB。
- 302站均得到完整68项曲线输入；比较2–5组，轮廓系数最高方案约0.427。精确数值见处理后评价表。
- 13项unittest及Streamlit AppTest检查通过：数据异常、重复冲突、零值、天气完整性、小时日均、方向比例、峰段边界、六页及三个出行模式、站点筛选、异常日空状态。
- 首次测试发现地图合并了重复站名列，已改为仅合并站点ID和指标，保留了原始失败日志及修复记录。
- Streamlit本版本的AppTest对单选分段控件连续状态序列化有限制，页面测试改为独立初始状态；真实切页和标签交互另以浏览器检查。

## 浏览器、便携环境与材料验证

- 六页及三个出行模式已经真实浏览器检查，截图S01—S11保存在screenshots；S03合并证明地图和排名，不重复生成S04。热力图短日期被自动识别为年份的问题已修复，分组代表站按与组中心曲线的距离选择。
- 1280像素桌面布局以及375像素窄屏收起侧栏后的阅读已检查，临时浏览器尺寸已恢复。
- CSV下载按钮已实际点击，PNG图表按钮显示“Snapshot succeeded”及正确文件名。当前内置浏览器工具没有返回下载文件落盘路径，因此不声称已用Excel打开浏览器下载文件；最终异机验收还需检查CSV/PNG实际文件。程序生成的CSV字节另作数值核对。
- 解压到“.work/移机 模拟/沪上流动”，验证CPython与全部导入路径都在包内；六页、三个模式无异常。真实启动器成功启动并关闭自身服务。该自动检查替换了Windows提示框与打开浏览器调用，用自动确认代替人工点击，其余启动逻辑实际执行。详情见AI过程中的JSON及日志。
- 6份DOCX按原模板结构生成并通过章节、图片、图表题注、字体、页边距和表格宽度检查；4份调研正文汉字数为1755—1818，每份5篇参考资料。未把AI草稿冒充成员独立写作。
- PPT共12页，已逐页渲染检查并修复轮廓系数被自动显示成“427m”的数字格式问题。
- Word渲染工具因缺少LibreOffice/soffice报WinError 2，因此未完成DOCX逐页视觉校对。已按documents技能允许的回退执行结构检查，并保留这个限制。

## 正式提交前仍须完成

- 另一台实际Windows 10/11 x64电脑：普通权限、无预装Python、断网，完整解压，人工双击启动、操作全部六页、导出CSV/PNG并查看文件、点击确定关闭服务。记录机器、系统、日期、执行人，补拍S12。
- 补齐4人学号；各成员阅读自己的调研、来源和对应代码，记录实际核验与修改。
- 在Word中逐页检查封面、换页、图表和题注；删除待填标记前必须先补齐真实信息。
- 按实际最终文件核对代码ZIP≤30,000,000字节、总包与邮件命名、收件人和截止时间。包内材料说明不代表已经发送。

学号、实际成员核验、答辩正式时长仍待补充；没有捏造这些结果。课程材料准备完毕也不代表已发送邮件。
